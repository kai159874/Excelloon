
<%= @balloon.content %>
<%= @balloon.color_status %>

<%= image_tag @balloon.balloon_image if @balloon.balloon_image.attached? %>

<%= link_to "バルーンを削除する", balloon_path(@balloon), method: :delete, class: "btn border", local: true %>